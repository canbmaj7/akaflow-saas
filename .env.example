# AkaFlow — ortam değişkenleri özeti
# Detay: backend/.env.example ve frontend/.env.example

# ── Backend (backend/.env) ──────────────────────────────────────
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Faz 3 — SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Faz 4 — AI Agent
GOOGLE_API_KEY=
DATABASE_URL=

# ── Frontend (frontend/.env veya .env.local) ───────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# API_URL=   # sadece Docker/Coolify production
